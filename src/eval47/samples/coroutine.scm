(define (application-start)
  (define (inner-function)
    (display "second line\n")
    (yield)
    (display "last line\n"))
  (display "first line\n")
  (spawn inner-function)
  (display "third line (main task should have quitted)\n"))
